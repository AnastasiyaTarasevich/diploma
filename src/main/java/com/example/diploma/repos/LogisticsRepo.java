package com.example.diploma.repos;
import com.example.diploma.models.Logistic;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogisticsRepo extends JpaRepository<Logistic, Long> {
}
